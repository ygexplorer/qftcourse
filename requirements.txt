# ──────────────────────────────────────────
# 规范场课程网站 — Python 依赖
# ──────────────────────────────────────────

# Django 核心框架
Django==5.1.7
asgiref==3.11.1
sqlparse==0.5.5

# 生产环境 WSGI 服务器
gunicorn==25.3.0

# 内容渲染：Markdown → HTML
Markdown==3.10.2

# 环境变量管理（从 .env 文件读取）
python-dotenv==1.2.2

# Excel 文件读取（学生批量导入）
pandas>=2.0
openpyxl>=3.1
xlrd>=2.0

# 生产数据库驱动（PostgreSQL）
# 本地开发用 SQLite，不需要此包
# 部署到腾讯云时取消下行注释：
# psycopg2-binary==2.9.10
